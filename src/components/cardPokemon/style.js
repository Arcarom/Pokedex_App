import {StyleSheet} from 'react-native';

export const Styles = StyleSheet.create({
  View: {
    padding: 20,
    alignItems: 'center',
    borderRadius: 20,
    width: '100%',
    height: 400,
    backgroundColor: '#fff',
  },
});
